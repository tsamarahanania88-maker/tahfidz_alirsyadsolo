rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // General match for any collection
    match /{document=**} {
      allow read, write: if true; // In production, replace with proper Auth rules
    }
  }
}
